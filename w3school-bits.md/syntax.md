* A Go syntax means the set down rules every code must follow to make it executeable

* they include 
1. package declaration
2. import package
3. functions
4. statement and expression

Example

package main
import ("fmt")

func main() {
  fmt.Println("Hello World!")
}